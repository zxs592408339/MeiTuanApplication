<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.administrator.myapplication">

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MeiTuanShopActivity" />
        <activity android:name=".MeiTuanMySelfActivity" />
        <activity android:name=".MeiTuanMoreActivity" />
        <activity android:name=".MeiTuanMainActivity" />
        <activity android:name="com.example.administrator.login.LoginActivity" />
        <activity android:name="com.example.administrator.choosecity.CityChooseActivity">
            <intent-filter>
                <action android:name="it.intent.action.SENDE" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name=".TabBarActivity" />
        <activity
            android:name="com.example.administrator.guidance.GuidanceActivity"
            android:theme="@style/NotitleActivityTheme" />
        <activity android:name="com.example.administrator.search.SearchShopActivity" />
    </application>

</manifest>